# Smoke test — minimal test to verify the harness works
- name: "smoke test - agent responds"
  agent: "dev"
  prompt: "What is your role? Answer in one sentence."
  timeout_ms: 60000
  assert:
    min_length: 10
